{
  "name": "ahg-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && mv dist ..",
    "install-frontend": "cd frontend && npm install"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
